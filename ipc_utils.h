#pragma once

#include "settings.h"
#include <sys/types.h>

key_t generate_key();

// Dijkstra's semaphore implementation

//int sem_init(int sem, int val);

//int sem_up(int sem, int val);

//int sem_down(int sem, int val);
